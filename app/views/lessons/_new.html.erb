
    <%= form_for(lesson, :url =>"/courses/#{lesson.course_id}/lessons") do |f| %>
    <% if !flash[:error].nil? %>
        <div id="error_explanation">
          <h2><%= pluralize(flash[:error].count, "error") %> prohibited this lesson from being saved:</h2>
    
          <ul>
          <% flash[:error].full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
     <% end %>
    <div class="form-grouped">
        <div class="form-group">
            <%= f.label :name %>
            <%= f.text_field :name , class: "form-control" %>
        </div>
        <div class="form-group">
            <%= f.label :description %>
            <%= f.text_area :description,  class: "form-control input-lg" %>
        </div>
        <div class="form-group">
            <%= f.label :link %>
            <%= f.text_field :link, class: "form-control" %>
        </div>
        <div class="text-center">
        <%= f.submit "Add lesson", class: "btn btn-default" %>
        </div>  
    </div>
    <%end%>

