<!-- Navigation -->
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header page-scroll">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand page-scroll" href="#page-top">Microcourses Platform</a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-right">
                    <li class="hidden">
                        <a href="#page-top"></a>
                    </li>
                    <li>
                        <%= link_to "Browse courses", courses_path, class: 'page-scroll' %>
                    </li>
                    <% if user_signed_in? && current_user.teacher? %>
                        <li>
                           <%= link_to "My course", my_courses_path, class: 'page-scroll' %>
                        </li>
                    <% end %>
                    <% if user_signed_in? %>
                    <li>
                        <%= link_to "Quizzes", my_quizzes_path %>
                    </li>
                    
                    <li>
                        <%= link_to "Sign out", destroy_user_session_path, :method => :delete , class: 'page-scroll' %>
                    </li>
                    <%else%>
                     <li>
                        <%= link_to "Sign in", new_user_session_path, class: 'page-scroll' %>
                    </li>
                    <li>
                       <%= link_to "Sign up", new_user_registration_path, class: 'page-scroll' %>
                    </li>
                    <%end%>
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-fluid -->
    </nav>